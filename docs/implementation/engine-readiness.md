# Integrated engine readiness

Authority: [Final Engine Specification](../architecture/engine-specification.md). Implementation follows its ordered stages; no research or product-policy decision was reopened. No deployment was performed.

Implementation and available integrated verification through stage 9 are complete. **Rollout is NO-GO.** Representative physical-mobile measurements and other explicitly listed validation gaps remain unresolved. The five-player task check is future formative validation and does not block these engineering gates.

## Ordered commits

| Stage | Commit | Result |
|---|---|---|
| 1 | `8ce2e5c` | Request, identity and result contracts; baseline evidence |
| 2 | `e97626d` | Explicit harmonic/subset compiler policies |
| 3 | `8fc68bd` | Complete resumable structural enumeration |
| 4 | `48bc47f` | Three-state Physical assessment |
| 5 | `5c0a7d5` | Deterministic classic ranking and ledgers |
| 6 | `86f0670` | Exact views, bounded sessions and replay pagination |
| 7 | `98ed648` | Versioned worker transport/lifecycle |
| 8 | `c001cde` | Atomic browsing, selection and MIDI playback cutover |
| 9 | This evidence commit | Integrated scope, cost and compatibility verification completed; failures and unavailable checks recorded |
| 10 | Conditional, not executed | Retirement requires passed engineering integration gates |

Stage 8 is the sole live cutover. Old search, hand-gate, scorer and boolean bridge sources remain available for offline controls, with a transitive architecture guard excluding them and the complete-collection adapters from the new browsing/worker roots. Stage 10 is not silently waived if a browser/performance gate remains unmet.

## Validation matrix

PASS below means the named implementation checks passed, not human anatomical accuracy or preference validation.

| IDs | Status | Evidence |
|---|---|---|
| V01–V02 | PASS | Independent Cartesian/pruning oracles; exact sets in `structuralGenerator.test.ts`, `subsets.test.ts`; old-policy census |
| V03–V06 | PASS | Import/metamorphic boundaries, identity codec, compiler table/overrides, independent literal close/drop construction |
| V07–V10 | Partial: v1 zero-provider checks pass | Physical boundary/cumulative/unsupported-profile tests, independent 15,625-pattern cover oracle, archived 88-case shadow; PASS/UNCERTAIN preserve S. V07 synthetic certificate scope/verifier fixtures have not been executed |
| V11–V12 | PASS | All 20 weights/features on 1,647,041 identical baseline survivors; 45,864 term checks; permutation/ledger/slash tests |
| V13 | PASS | Node/Chrome/Firefox/WebKit byte equality for 25 cases/2,597 rows plus complete geometry-pair, threshold, tie and Physical-boundary vectors |
| V14–V16 | PASS | Independent filters, varied page sizes/order, cached/replay equality, every-node small-domain checkpoint/resume tests and worker partial/pause/cancel checks |
| V17–V18 | PASS | Fake-worker races/controller tests, actual UI retention/context tests and exact repeated-MIDI audio spies; explicit worker failure/retry and stale-operation rejection |
| V19–V20 | PASS for measured scope | Full 480-request integrated census and all baseline allocation retention; forced tiny-cache and larger finite replay fixtures; enforced buffer bounds |
| V21 | Partial: Chrome latency passes; physical mobile unavailable | Complete release Chrome matrix; Firefox/WebKit conformance, UI smoke and diagnostic costs only. Physical-mobile, repeated Firefox/WebKit latency and complete heap attribution require separate evidence |
| V22 | Partial: disabled v1 runtime checks pass | Empirical manifest false; transitive production boundary guards; worker failures have typed errors, no synchronous/old-pool fallback. The specified mocked optional-extension failure with disabled-ID/ledger equality has not been executed; future activation remains unimplemented and disabled |
| V23 | PASS | Full current repository suite: 632 passed, 34 existing skips; separate recognition/SCALE/progression semantics retained |
| V24 | PASS for tested browser interactions | Chrome 30 UI assertions; Firefox/WebKit 7 each on the release UI; keyboard/touch/focus/reflow, uncertainty reasons, omissions, defaults and full ledger. No full assistive-technology study claimed |

Sources: [census](engine-integrated-census.json), [ranking control](engine-ranking-control.json), [physical shadow](engine-physical-shadow.json), [session/replay](engine-session-check.json), [worker smoke](engine-worker-browser.json), [worker costs/heaps](engine-worker-cost.json), [cross-runtime](engine-cross-runtime.json), [Chrome UI](engine-ui-browser.json), [Firefox/WebKit release UI](engine-release-runtime-ui.json), [complete Chrome release matrix](engine-ui-performance.json).

The cross-runtime harness aggregates many fixture outputs for comparison; its large comparison payload is an offline harness artifact, not the production message protocol. Production message-budget checks use the actual engine worker.

## Deliberate behavior changes

All totals below sum request/allocation pairs across 20 qualities × 12 roots × 2 contexts; they are not globally unique chord counts. Admission categories overlap.

| Integrated default census | Count |
|---|---:|
| Structural allocations / survivors | 15,943,679 |
| PASS | 583,916 |
| UNCERTAIN | 15,359,763 |
| REJECT | 0 |
| Baseline production allocations retained | 1,647,041 / 1,647,041 |
| Dominant-11 formula-policy additions | 1,790,745 |
| Former old-hand exclusions now reachable as UNCERTAIN | 12,749,194 |

Crossings, repetitions and former heuristic exclusions remain reachable. Dominant-11 permits the specified third omission; diminished-7 uses pitch-preserving `bb7` labels. Exact close/drop policies are literal MIDI predicates, while named legacy adapters disclose their old bass-only behavior. Physical warnings never become ranking gates. Slash/bass constraints cannot be purchased with score points.

On identical baseline survivors, all pinned weights/features match; span quantization changes scores by at most 0.0001650963 points and tuple tie-breaking deliberately changes order. Comparing the old pool/order with the final larger pool/order, top-6 changes in 318/480 requests and top-18 in 469/480. Of old top-6 entries, 624/2,880 move beyond six; 2,225/8,640 old top-18 entries move beyond eighteen. Maximum old-survivor demotion is 254,958 positions. These are distribution diagnostics, not preference improvements.

The final top-18 slices include 538 high-position entries versus 448 before, 5,305 open-string entries versus 4,556, and zero rootless entries versus eight; all 499,258 old rootless survivors are still reachable. The full evidence reports 183 protected slices, including every quality/context, density, actual bass/top MIDI and tone. This disclosed result does not justify silently retuning the frozen classic policy. There are 93 UNCERTAIN entries in the final top-6 and 793 in top-18; status does not filter or break rank ties.

UI pages replace six initial cards with twelve subsequent cards. Selection is separate, can remain outside filters/pages, and is visibly stale during revalidation. Cursor history is bounded to 64 entries with First page navigation. Drag preview stays local until release. Audio receives the exact MIDI multiset, and request/cancel tokens prevent late asynchronous playback.

## Measurements and engineering fixes

The largest standard request retains 8,650,752 bytes of packed buffers and accounts for 10,813,440 bytes including reserved indices, below 32 MiB. Forced replay retains no pool prefix and preserves exact results. Single-run Node session/census timings and concurrent-load browser component diagnostics are diagnostic only.

Chrome functional UI smoke observed at most 12 cards and 93,147 bytes per production message. The earlier real worker smoke measured cancellation acknowledgement at 4.1 ms and direct lookup at 0.3 ms; these single samples do not substitute for required percentiles. Browser diagnostics separately measure setup, enumeration, assessment, features/scoring, packing, heap accumulation and rich-page serialization/clone costs.

Release testing found and corrected WebKit focus restoration: `ChordDialog` now receives the actual opener ref instead of assuming mouse activation focuses that button. Both Firefox and WebKit then passed all seven checks; the evidence retains the original failure/build and successful rerun. Full source checks after the correction pass.

Thirty preoptimization samples of the widest C dominant-11 accompaniment request failed the desktop page/filter target: p95 246.4/228.6 ms. The correction replaces `Array.from` and its callback in the fixed six-byte packed decoder with six direct reads into a fresh array. It preserves strict structural/view validation, snapshot ownership, complete scans, candidates, scores and budgets. An 8,200-row test covers all supported signed frets, chunk boundaries and snapshot independence; cached/replay tests pass. Cross-runtime result SHA256 remains exactly `456e239f2c29218f4f19f8aead57bc821678a7d46e35d0f9d9c0972da1cd356d`.

The final release matrix contains **480 requests × 30 = 14,400** first-page measurements and **40 widest quality/context fixtures × 30 = 1,200** measurements each for page, filter and card selection. Its two disjoint segments have identical source, build, browser, hardware and conditions; the merge verifies thirty unique repeats per request, every census count, PASS+UNCERTAIN=S, zero REJECT and resource ceilings. Preoptimization and interrupted data are preserved separately and excluded from final percentiles. Final-page navigation also now disables Next when `hasMore` is false, covered by a focused rendering regression.

Measurements use Chrome 152.0.7977.84 headless, Windows 10.0.26200, AMD Ryzen 5 7500F (12 logical processors), 32 GB RAM, 1440×1000 viewport, a release Next server and a new worker per root change with warmed HTTP cache. Build: `pD6hcypTIf2hd57H3xuV3`; tracked source SHA256: `eff7eeec8b2a0395a214454a52e72249fcdbc8c0a337495a9e824088d3862216`. Current source/build identity was reverified before staging. End-to-end timing includes byte-count instrumentation and a paint frame after the exact result, fresh selection and enabled Play appear. Actual audio onset is not timed.

| UI action | Samples | Aggregate p50 / p95 / max (ms) | Worst request p95 (ms) |
|---|---:|---:|---:|
| First exact page/audio snapshot | 14,400 | 121.1 / 418.5 / 1,035.3 | 1,012.7 |
| Next page | 1,200 | 25.4 / 60.3 / 122.3 | 122.1 |
| Filter change | 1,200 | 23.4 / 54.1 / 119.6 | 119.5 |
| Already materialized card selection | 1,200 | 11.9 / 12.3 / 19.0 | 13.9 |

The worst first-page/page/filter request is C dominant-11 accompaniment; the worst selection fixture is E diminished standalone. **Zero per-request desktop p95 budget violations and zero observed main-thread long tasks** occurred in the measured windows. Maximum release-UI message: 96,420 bytes; maximum cards: 12. These results qualify the measured Chrome desktop scope only.

The long remaining-segment page series measured post-GC JS used heap of 4,391,072 bytes initially, 5,642,880 after 100 requests and 5,745,700 after 14,000 (maximum 5,953,736). Backing storage was 1,028,881 initially and 1,031,491 at 14,000. However, browser embedder heap grew from 2,934,648 bytes at 100 requests to 6,860,240 at 14,000. This is not a flat-total-memory result: engine attribution of that growth remains unresolved and needs separate profiling. The independent priority segment and the isolated worker series below are not combined into a single heap series.

A bounded [blank-target CDP control](engine-cdp-memory-control.json) ran 14,000 async resolved-object evaluations across 480 repeating fixture expressions in 3.89 seconds, using the same return-by-value/await-promise evaluation options and explicit GC. Embedder heap was 290,632 bytes initially and 290,616 at the end; JS heap was 420,428 and 423,228. Repeated evaluation alone did not reproduce the growth. This null result does not identify its cause: the control omits application DOM/rendering, workers, observers, audio and the approximately 35-minute measurement duration, so application/CDP interactions remain unexcluded.

Thirty distinct direct LOOKUP requests per C-major standalone and C dominant-11 accompaniment fixture, including refreshed assessment, transport and main-thread schema validation, measured p95 0.50/0.60 ms. These are lookup timings, separate from selecting an already materialized UI card.

Actual post-GC CDP measurements for the widest completed pool: worker used heap 777,216 bytes and backing storage 8,758,683 bytes. After 100 completed small-domain query/context/view changes and returning to the same widest request, backing storage was unchanged and used heap was 948,984 bytes (+171,768); the isolated harness page grew 55,204 bytes. These are runtime/bundle/harness-inclusive measurements, not exact engine-only attribution or an exhaustive leak proof.

The fret-36 forced-replay fixture paused truthfully at 10,008.7 ms after 2,910,857 structural allocations, with zero retained/accounted row buffers. Post-GC worker used heap was 700,880 bytes and backing storage 107,951 bytes. No exact total was claimed at that pause. The largest production-protocol message in these worker measurements was 232,015 bytes, below 1 MiB. Earlier complete replay and checkpoint tests establish exact eventual traversal under continued computation.

The full Node census preceded the fixed-byte decoder optimization. Its source hashes are preserved; the focused decoder proof, unchanged cross-runtime output hash and all browser requests' census-count assertions validate the equivalent final decoder without rewriting historical evidence.

## Reproducibility and remaining restrictions

- `npm test`: 632 passed, 34 existing skipped tests, 86 files.
- `npx tsc --noEmit --incremental false --pretty false`: PASS in the current workspace.
- `npm run build`: PASS; no `ignoreBuildErrors` or changed TypeScript exclusions.
- `npx eslint src --max-warnings 0`: PASS. Integration scripts are checked separately with zero warnings.
- Full repository ESLint: FAIL with 271 errors in pre-existing `.research` artifacts; no errors outside that directory in the recorded run. Downloaded `.tmp-engine-browsers` tools are explicitly excluded from source lint and Git; frozen research is not excluded to create an all-green claim.

Previously present untracked `docs/research` and some offline comparison files became unavailable during this shared-workspace run. No authored implementation edit removed or rewrote them. Earlier checks verified their recorded hashes, including the 88-case shadow, but current fresh verification cannot read the missing originals. Nine pinned legacy production-source hashes still verify. The original full typecheck had archived comparator errors; current success reflects the changed external file inventory, not repairs to frozen research or concealed exclusions. Restore the exact original artifacts to reproduce those archived commands.

No appropriate human validation has been performed. Product and release claims of improved preference, human playability or comfort remain prohibited; engineering conformance and any future formative check cannot substantiate them. Empirical ranking remains disabled. Deployment requires a subsequent user instruction.

The V07 synthetic certificate-verifier and V22 mocked optional-extension failure subchecks remain unexecuted. There is no runtime certificate provider or optional-model implementation to exercise. The implemented empty registry and disabled manifest are tested, but do not substitute for those stated future-extension scenarios or justify marking the complete rows passed.

## Integration decision

**NO-GO for rollout and conditional stage-10 retirement.** A complete engineering-gate pass is not established: physical-mobile measurements are unavailable, browser embedder-heap growth remains unattributed, the two validation subchecks above remain unexecuted, and fresh verification of three missing frozen source reviews is unavailable. Browser and test results are scoped to the evidence stated here; unrelated repository lint failures are recorded rather than hidden.

The integrated implementation through stage 9 remains reviewable. Its only live cutover occurred in stage 8. No candidate cap, monotonicity requirement, hand gate, empirical model or hidden legacy fallback was introduced to meet resource targets. Retained old pipeline files are excluded from the new live dependency graph and remain pending the specification's conditional retirement stage.

The five-player task check is future formative validation and is **not** a reason for this NO-GO. The prohibition on unsupported preference, human-playability and comfort claims remains in force independently of any future rollout decision.
