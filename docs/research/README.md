# Frozen research records

Research is closed. The documents and final evidence in this directory are frozen reference material and are never imported by production code.

Stage 10 moved acquisition, analysis, and comparison programs to the ignored local archive at `.research/stage-10-archive/scripts/`, preserving their original relative paths. References inside frozen study records that still name `scripts/physical-envelope-v2`, `scripts/web-voicing-corpus-v2`, `scripts/preference`, or `scripts/reference` describe their pre-cleanup location. Resolve those paths beneath `.research/stage-10-archive/` for local historical inspection. The frozen study files themselves were not rewritten to conceal that move or to change their recorded hashes.

The authoritative product rules are in `docs/architecture/product-policy-v2.md`; current production behavior does not execute these research programs or read their artifacts.
